@using Models;
@using System.Collections.Generic;
@model List<TodoItem>

@foreach(TodoItem i in Model)
{
	<div>
		Title: @(i.Title)
	</div>
	<div>
		Description: @(i.Description)
	</div>
	<div>
		Done?: @(i.IsDone)
	</div>
	<hr />
}